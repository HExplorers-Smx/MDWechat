plugins {
    id 'com.android.application' version '9.2.1' apply false
    id 'com.android.library' version '9.2.1' apply false
}

// AGP 9.x includes built-in Kotlin support for Android modules, so this
// project intentionally does not apply org.jetbrains.kotlin.android anymore.
// Module-specific Android configuration lives in each module's build.gradle.
